# .env.example - Copy this to .env and fill in your actual keys

# --- Backend AI Provider API Keys ---
# Get keys from the respective providers
OPENAI_API_KEY="sk-..."
ANTHROPIC_API_KEY="sk-ant-..."
GOOGLE_API_KEY="AIza..."
# Add other providers as needed (e.g., COHERE_API_KEY)

# --- Forwarder Service Configuration ---
# List of API keys allowed to use *this* forwarder service (comma-separated)
# Generate these using the `generate-key` command: python -m app.main generate-key
FORWARDER_API_KEYS="fwd_key_replace_me_1,fwd_key_replace_me_2"

# Optional: Base URLs if not using standard ones or for self-hosted models
# OPENAI_BASE_URL="https://api.openai.com/v1"
# ANTHROPIC_BASE_URL="https://api.anthropic.com/v1"
# GOOGLE_BASE_URL="https://generativelanguage.googleapis.com/v1beta"
